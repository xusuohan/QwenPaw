# 性能优化 Phase 4b：§4.1 模型客户端缓存（工厂级指纹缓存）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `create_model_and_formatter` 加进程内缓存，按 config 指纹复用内层 `OpenAIChatModelCompat`（及其热 httpx 连接池），消除每请求重建 LLM 客户端 + TLS 握手的开销，降低运行时 TTFT。

**Architecture:** agentscope 的 `OpenAIChatModel` 在 `__init__` 即建 `openai.AsyncClient`（`_openai_model.py:158-167`），`__call__` 复用同一 client → 缓存 model 实例 = 复用热池。指纹 key = `(provider_id, model_id, base_url, api_key, json.dumps(generate_kwargs, sort_keys=True))`，任何配置变 → 指纹变 → 自动 miss → 新 client，**stale 结构性不可能，免失效钩子**。只缓存内层 model；`TokenRecordingModelWrapper` + `RetryChatModel` + formatter 每请求新建（行为零变化，C4 满足：Agent 仍每请求重建）。kill-switch flag `QWENPAW_PERF_MODEL_CLIENT_CACHE`（默认开）。

**Tech Stack:** Python 3.10、`threading.Lock`、pytest（asyncio_mode=auto）。

**对应 spec：** `docs/superpowers/specs/2026-06-23-模型客户端缓存-design.md`。
**前置：** Phase 1–5 已完成（HEAD `c68d695e`）；本 Phase 从 `feature/usb-portable` 当前 HEAD 继续，不合并分支。
**行号基准：** 截至 HEAD `c68d695e`，改动后会漂移（PROGRESS_BASELINE §8 坑-6），动手前用 grep 重新定位。

---

## 范围说明（scope check）

单一子系统（model_factory.py 的客户端缓存 + 其单测）。spec 已界定 in/out scope：只缓存 client，不动 `load_agent_config` 读盘、不缓存 wrapper/formatter、不碰 Agent 生命周期。一个计划即可。

---

## File Structure（Phase 4b）

| 文件 | 改动 |
|---|---|
| `src/qwenpaw/agents/model_factory.py` | Task 1：`import json` + flag/指纹 helper；Task 2：`import threading` + 缓存 store + `_get_cached_inner_model`/`clear`/`stats`；Task 3：cap + 驱逐；Task 4：`create_model_and_formatter` 两分支走缓存 |
| `tests/unit/agents/test_model_client_cache.py` | Task 1–4 逐步新增（fakes + 10 测试） |

**唯一改动源码文件** = `model_factory.py`（工厂级放置，blast radius 最小）。

---

## Task 1: 缓存开关 + 指纹 helper（纯函数）

**Files:**
- Modify: `src/qwenpaw/agents/model_factory.py:13-16`（加 `import json`）+ 顶部 helper 区（imports 之后、`def _file_url_to_path` 之前）
- Create: `tests/unit/agents/test_model_client_cache.py`

- [ ] **Step 1: 加 `import json`**

`model_factory.py` 当前顶部（line 13-17）：
```python
import base64
import logging
import os
from typing import List, Sequence, Tuple, Type, Any, Union, Optional
```
改为（按字母序插 `json`）：
```python
import base64
import json
import logging
import os
from typing import List, Sequence, Tuple, Type, Any, Union, Optional
```

- [ ] **Step 2: 写失败测试（flag + 指纹）**

创建 `tests/unit/agents/test_model_client_cache.py`（**只导入本 Task 用到的名字**——`clear_model_client_cache` / `mf` / `types` / `threading` 在后续 Task 定义或用到时再加，否则 collection 失败或 pylint unused-import，§7.2）：
```python
# -*- coding: utf-8 -*-
"""Tests for the model-client cache in qwenpaw.agents.model_factory (§4.1)."""
from __future__ import annotations

import pytest

from qwenpaw.agents.model_factory import (
    _model_client_cache_enabled,
    _model_client_fingerprint,
)


class _FakeModel:
    """Minimal stand-in for an inner chat model (identity-comparable)."""

    def __init__(self, tag: str) -> None:
        self.tag = tag


class _FakeProvider:
    """Stand-in provider exposing the fingerprint surface + build counter."""

    def __init__(
        self,
        base_url: str = "https://x.example",
        api_key: str = "k",
        generate_kwargs=None,
    ) -> None:
        self.base_url = base_url
        self.api_key = api_key
        self._generate_kwargs = generate_kwargs or {}
        self.build_calls = 0

    def get_effective_generate_kwargs(self, model_id: str):
        return dict(self._generate_kwargs)

    def get_chat_model_instance(self, model_id: str):
        self.build_calls += 1
        return _FakeModel(f"{self.base_url}|{model_id}|{self.api_key}")


class TestCacheFlag:
    def test_default_on(self, monkeypatch):
        monkeypatch.delenv("QWENPAW_PERF_MODEL_CLIENT_CACHE", raising=False)
        assert _model_client_cache_enabled() is True

    @pytest.mark.parametrize("val", ["0", "false", "no", "off", "FALSE"])
    def test_disabled_values(self, monkeypatch, val):
        monkeypatch.setenv("QWENPAW_PERF_MODEL_CLIENT_CACHE", val)
        assert _model_client_cache_enabled() is False

    @pytest.mark.parametrize("val", ["1", "true", "yes"])
    def test_enabled_values(self, monkeypatch, val):
        monkeypatch.setenv("QWENPAW_PERF_MODEL_CLIENT_CACHE", val)
        assert _model_client_cache_enabled() is True


class TestFingerprint:
    def test_same_config_equal(self):
        p = _FakeProvider(api_key="k")
        assert _model_client_fingerprint(p, "p1", "m1") == (
            _model_client_fingerprint(p, "p1", "m1")
        )

    def test_different_api_key_differs(self):
        p = _FakeProvider(api_key="k1")
        a = _model_client_fingerprint(p, "p1", "m1")
        p.api_key = "k2"
        b = _model_client_fingerprint(p, "p1", "m1")
        assert a != b

    def test_different_generate_kwargs_differs(self):
        p = _FakeProvider(generate_kwargs={"temperature": 0.5})
        a = _model_client_fingerprint(p, "p1", "m1")
        p._generate_kwargs = {"temperature": 0.9}
        b = _model_client_fingerprint(p, "p1", "m1")
        assert a != b

    def test_different_model_id_differs(self):
        p = _FakeProvider()
        assert _model_client_fingerprint(p, "p1", "m1") != (
            _model_client_fingerprint(p, "p1", "m2")
        )
```

- [ ] **Step 3: 跑测试确认全红**

```bash
pytest tests/unit/agents/test_model_client_cache.py -v
```
Expected: FAIL（`ImportError: cannot import name '_model_client_cache_enabled' ...`）。

- [ ] **Step 4: 实现 helper**

在 `model_factory.py` imports 之后、`def _file_url_to_path` 之前加：
```python
# ---------------------------------------------------------------------------
# Model-client cache (§4.1): reuse the warm httpx connection pool across
# requests instead of rebuilding OpenAIChatModelCompat (and its
# openai.AsyncClient) on every call. Keyed by a config fingerprint so any
# change to base_url / api_key / generate_kwargs yields a fresh client
# (stale is impossible by construction — no invalidation hooks needed).
# Toggle with QWENPAW_PERF_MODEL_CLIENT_CACHE (default on; 0/false/no/off).
# ---------------------------------------------------------------------------

_CACHE_DISABLE_VALUES = frozenset({"0", "false", "no", "off"})


def _model_client_cache_enabled() -> bool:
    """Cache is ON by default.

    Disable via ``QWENPAW_PERF_MODEL_CLIENT_CACHE`` set to one of
    ``{0, false, no, off}`` (case-insensitive).
    """
    raw = os.environ.get("QWENPAW_PERF_MODEL_CLIENT_CACHE")
    if raw is None:
        return True
    return raw.strip().lower() not in _CACHE_DISABLE_VALUES


def _model_client_fingerprint(provider, provider_id, model_id) -> tuple:
    """Stable, hashable key capturing every input that determines the built
    chat-model client.

    ``base_url`` also pins ``default_headers`` (they are a pure function of
    base_url in ``get_chat_model_instance``); ``generate_kwargs`` is included
    so per-model parameter edits are seen as a miss.
    """
    return (
        provider_id,
        model_id,
        provider.base_url,
        provider.api_key,
        json.dumps(
            provider.get_effective_generate_kwargs(model_id),
            sort_keys=True,
            default=str,
        ),
    )
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
pytest tests/unit/agents/test_model_client_cache.py -v
```
Expected: PASS（TestCacheFlag + TestFingerprint 全绿）。

- [ ] **Step 6: Commit**

```bash
git add src/qwenpaw/agents/model_factory.py tests/unit/agents/test_model_client_cache.py
git commit -m "perf(model_factory): §4.1 cache flag + config fingerprint helpers

Co-Authored-By: Claude <noreply@anthropic.com>"
```
（pre-commit 跑；re-stage 若 reformats。`M src/qwenpaw/cli/desktop_cmd.py` 勿动——只 add 本 Task 的两个文件。）

---

## Task 2: 缓存 store + `_get_cached_inner_model` + clear/stats

**Files:**
- Modify: `src/qwenpaw/agents/model_factory.py`（加 `import threading` + store + 三个函数）
- Modify: `tests/unit/agents/test_model_client_cache.py`（加 store 测试）

- [ ] **Step 1: 加 `import threading`**

`model_factory.py` 顶部（Task 1 后）：
```python
import base64
import json
import logging
import os
```
改为：
```python
import base64
import json
import logging
import os
import threading
```

- [ ] **Step 2: 写失败测试（store 行为）**

在文件顶部 import 区追加本 Task 用到的名字（可与 Task 1 的 from-import 并列为第二个块）：

```python
import json
import threading

from qwenpaw.agents.model_factory import (
    _get_cached_inner_model,
    clear_model_client_cache,
    model_client_cache_stats,
)
```

再在文件末尾追加测试类：

```python
class TestCachedInnerModel:
    def setup_method(self):
        clear_model_client_cache()

    def teardown_method(self):
        clear_model_client_cache()

    def test_cache_hit_returns_same_instance(self):
        prov = _FakeProvider()
        a = _get_cached_inner_model(prov, "p1", "m1")
        b = _get_cached_inner_model(prov, "p1", "m1")
        assert a is b
        assert prov.build_calls == 1

    def test_different_api_key_misses(self):
        prov = _FakeProvider(api_key="k1")
        a = _get_cached_inner_model(prov, "p1", "m1")
        prov.api_key = "k2"
        b = _get_cached_inner_model(prov, "p1", "m1")
        assert a is not b
        assert prov.build_calls == 2

    def test_different_generate_kwargs_misses(self):
        prov = _FakeProvider(generate_kwargs={"temperature": 0.5})
        a = _get_cached_inner_model(prov, "p1", "m1")
        prov._generate_kwargs = {"temperature": 0.9}
        b = _get_cached_inner_model(prov, "p1", "m1")
        assert a is not b
        assert prov.build_calls == 2

    def test_shared_across_providers_same_fingerprint(self):
        # Two distinct provider instances with identical config share one
        # cached client (the warm pool is process-global by fingerprint).
        prov_a = _FakeProvider(api_key="k")
        prov_b = _FakeProvider(api_key="k")
        a = _get_cached_inner_model(prov_a, "p1", "m1")
        b = _get_cached_inner_model(prov_b, "p1", "m1")
        assert a is b

    def test_flag_disabled_passthrough(self, monkeypatch):
        monkeypatch.setenv("QWENPAW_PERF_MODEL_CLIENT_CACHE", "0")
        prov = _FakeProvider()
        a = _get_cached_inner_model(prov, "p1", "m1")
        b = _get_cached_inner_model(prov, "p1", "m1")
        assert a is not b
        assert prov.build_calls == 2
        assert model_client_cache_stats()["size"] == 0

    def test_no_stale_on_provider_mutation(self):
        prov = _FakeProvider(api_key="old")
        a = _get_cached_inner_model(prov, "p1", "m1")
        prov.api_key = "new"  # simulate update_config() in-place change
        b = _get_cached_inner_model(prov, "p1", "m1")
        assert a is not b
        assert b.tag != a.tag  # built from the new api_key

    def test_stats_never_expose_key(self):
        prov = _FakeProvider(
            api_key="SECRET_KEY",
            base_url="https://secret.example",
        )
        _get_cached_inner_model(prov, "p1", "m1")

        blob = json.dumps(model_client_cache_stats())
        assert "SECRET_KEY" not in blob
        assert "secret.example" not in blob
        assert model_client_cache_stats()["size"] == 1
        assert model_client_cache_stats()["misses"] == 1
        assert model_client_cache_stats()["hits"] == 0

    def test_concurrent_build_safe(self):
        prov = _FakeProvider()
        barrier = threading.Barrier(8)
        results: list = []
        results_lock = threading.Lock()

        def worker():
            barrier.wait()
            m = _get_cached_inner_model(prov, "p1", "m1")
            with results_lock:
                results.append(m)

        threads = [threading.Thread(target=worker) for _ in range(8)]
        for t in threads:
            t.start()
        for t in threads:
            t.join()
        assert len(results) == 8
        assert all(r is results[0] for r in results)  # all share one client
        assert prov.build_calls == 1  # no duplicate build
```

- [ ] **Step 3: 跑测试确认新测全红**

```bash
pytest tests/unit/agents/test_model_client_cache.py::TestCachedInnerModel -v
```
Expected: FAIL（`ImportError: cannot import name '_get_cached_inner_model'`）。

- [ ] **Step 4: 实现 store + 三个函数**

在 `model_factory.py` 的 `_model_client_fingerprint` 之后加（**注意：本 Task 暂不加 cap 驱逐，留给 Task 3**）：
```python
_MODEL_CLIENT_CACHE: dict = {}
_MODEL_CLIENT_CACHE_LOCK = threading.Lock()
_MODEL_CLIENT_CACHE_HITS = 0
_MODEL_CLIENT_CACHE_MISSES = 0


def _get_cached_inner_model(provider, provider_id, model_id):
    """Return a (possibly cached) inner chat-model instance.

    On a miss the provider builds the client once; later calls with the same
    fingerprint reuse it (warm httpx pool). The build runs under the lock so
    concurrent same-key callers share a single build. When the cache is
    disabled (QWENPAW_PERF_MODEL_CLIENT_CACHE) this passes straight through to
    ``provider.get_chat_model_instance``, identical to pre-cache behavior.
    """
    if not _model_client_cache_enabled():
        return provider.get_chat_model_instance(model_id)

    fp = _model_client_fingerprint(provider, provider_id, model_id)
    global _MODEL_CLIENT_CACHE_HITS, _MODEL_CLIENT_CACHE_MISSES
    with _MODEL_CLIENT_CACHE_LOCK:
        cached = _MODEL_CLIENT_CACHE.get(fp)
        if cached is not None:
            _MODEL_CLIENT_CACHE_HITS += 1
            return cached
        # Build under the lock so concurrent same-key callers share it.
        built = provider.get_chat_model_instance(model_id)
        _MODEL_CLIENT_CACHE[fp] = built
        _MODEL_CLIENT_CACHE_MISSES += 1
        return built


def clear_model_client_cache() -> None:
    """Drop all cached clients and reset counters (tests + emergency reset)."""
    global _MODEL_CLIENT_CACHE_HITS, _MODEL_CLIENT_CACHE_MISSES
    with _MODEL_CLIENT_CACHE_LOCK:
        _MODEL_CLIENT_CACHE.clear()
        _MODEL_CLIENT_CACHE_HITS = 0
        _MODEL_CLIENT_CACHE_MISSES = 0


def model_client_cache_stats() -> dict:
    """Return ``{size, hits, misses, enabled}``.

    Never exposes key material (api_key / base_url).
    """
    with _MODEL_CLIENT_CACHE_LOCK:
        return {
            "size": len(_MODEL_CLIENT_CACHE),
            "hits": _MODEL_CLIENT_CACHE_HITS,
            "misses": _MODEL_CLIENT_CACHE_MISSES,
            "enabled": _model_client_cache_enabled(),
        }
```

- [ ] **Step 5: 跑测试确认全绿**

```bash
pytest tests/unit/agents/test_model_client_cache.py -v
```
Expected: PASS（含 `TestCachedInnerModel` 全部 8 个）。

- [ ] **Step 6: Commit**

```bash
git add src/qwenpaw/agents/model_factory.py tests/unit/agents/test_model_client_cache.py
git commit -m "perf(model_factory): §4.1 cached inner model store + stats (thread-safe)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: 容量上限 + 驱逐

**Files:**
- Modify: `src/qwenpaw/agents/model_factory.py`（`_get_cached_inner_model` 加驱逐 + 定义 `_MODEL_CLIENT_CACHE_CAP`）
- Modify: `tests/unit/agents/test_model_client_cache.py`（加驱逐测试）

- [ ] **Step 1: 写失败测试（驱逐）**

测试用 `monkeypatch.setattr(mf, ...)`，故先在文件顶部 import 区加（本 Task 首次用到）：

```python
import qwenpaw.agents.model_factory as mf
```

再在 `TestCachedInnerModel` 末尾追加：
```python
    def test_cap_evicts_oldest(self, monkeypatch):
        monkeypatch.setattr(mf, "_MODEL_CLIENT_CACHE_CAP", 3)
        prov = _FakeProvider()
        for i in range(5):
            _get_cached_inner_model(prov, "p1", f"m{i}")
        assert model_client_cache_stats()["size"] <= 3
```

- [ ] **Step 2: 跑测试确认红**

```bash
pytest tests/unit/agents/test_model_client_cache.py::TestCachedInnerModel::test_cap_evicts_oldest -v
```
Expected: FAIL（`AssertionError: assert 5 <= 3`——当前无驱逐，size=5）。

- [ ] **Step 3: 实现 cap + 驱逐**

在 `_MODEL_CLIENT_CACHE_MISSES = 0` 之后加常量：
```python
_MODEL_CLIENT_CACHE_CAP = 64
```

把 `_get_cached_inner_model` 内 store 段（Task 2 写的）：
```python
        built = provider.get_chat_model_instance(model_id)
        _MODEL_CLIENT_CACHE[fp] = built
        _MODEL_CLIENT_CACHE_MISSES += 1
        return built
```
改为：
```python
        built = provider.get_chat_model_instance(model_id)
        _MODEL_CLIENT_CACHE[fp] = built
        _MODEL_CLIENT_CACHE_MISSES += 1
        # Bound memory: evict oldest entries (dict preserves insert order).
        while len(_MODEL_CLIENT_CACHE) > _MODEL_CLIENT_CACHE_CAP:
            _MODEL_CLIENT_CACHE.pop(next(iter(_MODEL_CLIENT_CACHE)))
        return built
```

- [ ] **Step 4: 跑测试确认绿**

```bash
pytest tests/unit/agents/test_model_client_cache.py -v
```
Expected: PASS（含 `test_cap_evicts_oldest`）。

- [ ] **Step 5: Commit**

```bash
git add src/qwenpaw/agents/model_factory.py tests/unit/agents/test_model_client_cache.py
git commit -m "perf(model_factory): §4.1 bound model-client cache (FIFO cap 64)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: 接入 `create_model_and_formatter`（两分支走缓存）

**Files:**
- Modify: `src/qwenpaw/agents/model_factory.py:1006-1030`（`create_model_and_formatter` 的 model 解析段）
- Modify: `tests/unit/agents/test_model_client_cache.py`（加工厂级集成测试）

- [ ] **Step 1: 写失败测试（工厂级：内层缓存、wrapper 每请求重包）**

测试用 `types.SimpleNamespace`，先在文件顶部 import 区加（本 Task 首次用到）：

```python
import types
```

（`mf` 已在 Task 3 import。）再在测试文件末尾追加新类：
```python
class TestFactoryIntegration:
    """create_model_and_formatter routes the inner model through the cache
    while rebuilding the wrappers fresh each call (C4: agent rebuilt per
    request; only the client is cached)."""

    def setup_method(self):
        clear_model_client_cache()

    def teardown_method(self):
        clear_model_client_cache()

    def test_factory_caches_inner_rewraps_per_call(self, monkeypatch):
        prov = _FakeProvider(api_key="k")

        slot = types.SimpleNamespace(provider_id="p1", model="m1")
        running = types.SimpleNamespace(
            llm_retry_enabled=False,
            llm_max_retries=1,
            llm_backoff_base=0.1,
            llm_backoff_cap=1.0,
            llm_max_concurrent=4,
            llm_max_qpm=0,
            llm_rate_limit_pause=0.0,
            llm_rate_limit_jitter=0.0,
            llm_acquire_timeout=0.0,
        )
        cfg = types.SimpleNamespace(
            id="agent-x",
            active_model=slot,
            running=running,
        )
        # load_agent_config is imported function-local from config.config.
        monkeypatch.setattr(
            "qwenpaw.config.config.load_agent_config",
            lambda aid: cfg,
        )

        class _Mgr:
            def get_provider(self, pid):
                return prov if pid == "p1" else None

        monkeypatch.setattr(
            mf.ProviderManager,
            "get_instance",
            staticmethod(lambda: _Mgr()),
        )
        # Formatter construction is orthogonal to the cache; stub it.
        monkeypatch.setattr(
            mf,
            "_create_formatter_instance",
            lambda cls: object(),
        )

        m1, _ = mf.create_model_and_formatter(agent_id="agent-x")
        m2, _ = mf.create_model_and_formatter(agent_id="agent-x")
        # Wrappers (RetryChatModel) are freshly built each call...
        assert m1 is not m2
        # ...but the inner model was built only once (cache hit on 2nd call).
        assert prov.build_calls == 1
```

- [ ] **Step 2: 跑测试确认红**

```bash
pytest tests/unit/agents/test_model_client_cache.py::TestFactoryIntegration -v
```
Expected: FAIL（`assert prov.build_calls == 1` 失败 → `build_calls == 2`，因工厂仍每请求直调 `get_chat_model_instance`，未走缓存）。

- [ ] **Step 3: 改 `create_model_and_formatter` 两分支**

`model_factory.py` 当前（grep 重新定位 `# Create chat model from agent-specific or global config`，约 1006-1030）：
```python
    # Create chat model from agent-specific or global config
    if model_slot and model_slot.provider_id and model_slot.model:
        # Use agent-specific model
        manager = ProviderManager.get_instance()
        provider = manager.get_provider(model_slot.provider_id)
        if provider is None:
            raise ProviderError(
                message=f"Provider '{model_slot.provider_id}' not found.",
            )

        model = provider.get_chat_model_instance(model_slot.model)
        provider_id = model_slot.provider_id
    else:
        # Fallback to global active model
        model = ProviderManager.get_active_chat_model()
        global_model = ProviderManager.get_instance().get_active_model()
        if not global_model:
            raise ProviderError(
                message=(
                    "No active model configured. "
                    "Please configure a model using 'qwenpaw models config' "
                    "or set an agent-specific model."
                ),
            )
        provider_id = global_model.provider_id
```
改为：
```python
    # Create chat model from agent-specific or global config. The inner
    # model is resolved through _get_cached_inner_model so the warm httpx
    # connection pool is reused across requests (§4.1).
    if model_slot and model_slot.provider_id and model_slot.model:
        # Use agent-specific model
        manager = ProviderManager.get_instance()
        provider = manager.get_provider(model_slot.provider_id)
        if provider is None:
            raise ProviderError(
                message=f"Provider '{model_slot.provider_id}' not found.",
            )

        model = _get_cached_inner_model(
            provider,
            model_slot.provider_id,
            model_slot.model,
        )
        provider_id = model_slot.provider_id
    else:
        # Fallback to global active model. Replicates
        # ProviderManager.get_active_chat_model()'s validation so the build
        # goes through the cache; error messages are preserved exactly.
        manager = ProviderManager.get_instance()
        global_model = manager.get_active_model()
        if (
            global_model is None
            or global_model.provider_id == ""
            or global_model.model == ""
        ):
            raise ProviderError(
                message="No active model configured.",
            )
        provider = manager.get_provider(global_model.provider_id)
        if provider is None:
            raise ProviderError(
                message=(
                    f"Active provider '{global_model.provider_id}' "
                    f"not found."
                ),
            )
        model = _get_cached_inner_model(
            provider,
            global_model.provider_id,
            global_model.model,
        )
        provider_id = global_model.provider_id
```

（说明：原 `else` 分支的 `if not global_model` 带 "Please configure..." 长消息是**死代码**——`get_active_chat_model()` 在它之前就已对"无 active model"抛 `"No active model configured."`。本改动用 `get_active_chat_model` 的真实消息，移除死代码，行为不变。`_create_formatter_instance` + wrapper 段（其后 ~1032-1043）**不动**。）

- [ ] **Step 4: 跑工厂级测试 + import 冒烟**

```bash
pytest tests/unit/agents/test_model_client_cache.py -v
python -c "import qwenpaw.agents.model_factory as m; print('import OK')"
```
Expected: 全绿（含 `TestFactoryIntegration`）+ import OK。

- [ ] **Step 5: 定向回归（factory 调用方）**

```bash
pytest tests/unit/agents/ tests/unit/app/test_title_generator.py -q
```
Expected: 全 PASS（无回归）。

- [ ] **Step 6: Commit**

```bash
git add src/qwenpaw/agents/model_factory.py tests/unit/agents/test_model_client_cache.py
git commit -m "perf(model_factory): §4.1 route create_model_and_formatter through cache

Both agent-specific and global-fallback branches now resolve the inner
model via _get_cached_inner_model (warm httpx pool reuse). Wrappers and
formatter are still rebuilt per request (C4). Fallback validation now
mirrors ProviderManager.get_active_chat_model (removes dead branch).

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 5: 全量回归 + lint/typecheck

- [ ] **Step 1: 全量回归**

```bash
pytest tests/unit/agents/ tests/unit/app/ tests/unit/utils/ -q
python -c "import qwenpaw.agents.model_factory; print('import OK')"
```
Expected: 全 PASS + import OK。（`model_factory` 在 `agents/`，被 `app/` 的 runner/title_generator 等调用；`providers/` 反向依赖 `agents/`，不受影响，故不纳入。）

- [ ] **Step 2: lint/typecheck（pre-commit canonical，§8 坑-3）**

```bash
pre-commit run black flake8 pylint mypy --files src/qwenpaw/agents/model_factory.py tests/unit/agents/test_model_client_cache.py
```
Expected: Passed。本地 CLI 工具版本可能与 pre-commit 钉死版本不一致（§8 坑-3）——以 pre-commit 为准；本地 black 26.x / flake8 误报可忽略。若 pylint 提示 helper 缺类型注解，补最小注解（如 `provider: "object"`, `provider_id: str`, `model_id: str`）再 re-stage。

---

## Task 6: 最终评审 + 回写 PROGRESS_BASELINE

- [ ] **Step 1: 最终 code review（整个 Phase 4b）**

对 Phase 4b 全量改动做 code review（base = spec 提交 `b866d172`，head = Task 4 最终提交），确认：
- 缓存命中正确、指纹含 base_url+api_key+generate_kwargs、provider 变更自愈；
- 构建在锁内、并发安全、无重复构建；
- cap 驱逐生效、stats 不泄露 key；
- `create_model_and_formatter` 两分支均走缓存、wrapper/formatter 仍每请求新建、fallback 错误消息与 `get_active_chat_model` 一致；
- 开关关 = 直通、行为与改动前一致；
- 无回归。

- [ ] **Step 2: 回写 `docs/PROGRESS_BASELINE.md`**

按基准 §10 接手清单第 6 步：
- §0 一句话现状：注明 §4.1 已完成（模型客户端缓存）。
- §5 加 "Phase 4b — §4.1 模型客户端缓存"（含提交 SHA 范围 + kill-switch flag）。
- §6 删除 §4.1 行（剩余 §4.3/§4.5/§3.x/§5.x）。
- §9 关键文件索引：`agents/model_factory.py` 加入"结构性改动"；`tests/unit/agents/test_model_client_cache.py` 加入测试索引。

```bash
git add docs/PROGRESS_BASELINE.md
git commit -m "docs: PROGRESS_BASELINE — §4.1 模型客户端缓存完成（Phase 4b）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Phase 4b 完成准则

- `create_model_and_formatter` 内层 model 经 `_get_cached_inner_model` 缓存；同配置第 2 次起命中热池（0 构建 / 0 TLS）。
- 指纹含 `provider_id + model_id + base_url + api_key + generate_kwargs` → 任何配置变自愈，无 stale。
- wrapper（`TokenRecordingModelWrapper` + `RetryChatModel`）+ formatter 每请求新建；Agent 实例仍每请求重建（C4）。
- cap 64 FIFO 驱逐；`threading.Lock` 并发安全；`stats` 不泄露 key。
- kill-switch `QWENPAW_PERF_MODEL_CLIENT_CACHE` 默认开、可一键关 = 直通。
- 受影响模块测试全绿；pre-commit（black/flake8/pylint/mypy）通过。
- PROGRESS_BASELINE 已回写。

---

## 风险点（呼应 spec §8）

1. **指纹完整性**：已核实 `get_effective_generate_kwargs` 在基类 `Provider`（`provider.py:288`）、纯内存配置合并、6 类 provider 统一继承 → provider-agnostic 且 cheap；`update_model_config`（`provider.py:304`）改 per-model 参数也经指纹自愈。
2. **agentscope `AsyncClient` 跨并发流共享**：是其设计用途；极端并发下若有未知 bug → `QWENPAW_PERF_MODEL_CLIENT_CACHE=0` 一键关。
3. **fallback 分支重构**：`get_active_chat_model`（`provider_manager.py:2022`）经 grep 仅被工厂调用，重构安全；错误消息与之一致（移除死代码长消息）。
4. **构建持锁**：`provider.get_chat_model_instance` 仅构造客户端对象（无网络），持锁微秒级；无重入（不会回调工厂）→ `Lock` 足够。
5. **`load_agent_config` 读盘仍在**：§4.1 不消除（独立项）；收益纯来自省 httpx 构建 + TLS。
